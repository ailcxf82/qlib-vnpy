# 运行说明

## ✅ 问题已修复！

所有脚本已修复，现在可以从**任何目录**运行了！

## 🚀 运行方式

### 方式1：从项目根目录运行（推荐）

```bash
# 切换到项目根目录
cd D:\lianghuatouzi\Qlib1114

# 运行完整流程
python pipeline/run_all.py

# 或只运行训练
python pipeline/run_train.py

# 或只运行预测
python pipeline/run_predict.py

# 或运行回测
python backtest/run_vnpy_backtest.py
```

### 方式2：从子目录运行（也可以）

```bash
# 在pipeline目录下
cd D:\lianghuatouzi\Qlib1114\pipeline
python run_all.py

# 在backtest目录下
cd D:\lianghuatouzi\Qlib1114\backtest
python run_vnpy_backtest.py
```

### 方式3：使用绝对路径运行

```bash
# 从任何位置运行
python D:\lianghuatouzi\Qlib1114\pipeline\run_all.py
```

## 🔧 修复内容

已在以下文件中添加了自动路径处理：

- ✅ `pipeline/run_all.py`
- ✅ `pipeline/run_train.py`
- ✅ `pipeline/run_predict.py`
- ✅ `backtest/run_vnpy_backtest.py`

这些脚本现在会自动：
1. 找到项目根目录
2. 添加到Python路径
3. 切换工作目录

## 📝 当前配置

你的数据配置已更新为：
```yaml
qlib:
  provider_uri: "D:/qlib_data/qlib_data"
```

请确保此路径下有Qlib数据。

## 🎯 快速测试

现在你可以直接运行：

```bash
cd D:\lianghuatouzi\Qlib1114\pipeline
python run_all.py
```

或者从根目录：

```bash
cd D:\lianghuatouzi\Qlib1114
python pipeline/run_all.py
```

都可以正常工作！ ✨

## ⚠️ 注意事项

1. **确保已安装依赖**：
   ```bash
   pip install -r requirements.txt
   ```

2. **确保Qlib数据存在**：
   检查 `D:/qlib_data/qlib_data` 目录是否有数据

3. **首次运行可能需要时间**：
   - 因子计算需要处理大量数据
   - 模型训练需要几分钟到几小时
   - 请耐心等待

## 📊 运行输出

运行后会生成：
- `logs/` - 日志文件
- `data/models/` - 训练的模型
- `data/predictions/` - 预测结果
- `data/backtest_results/` - 回测报告

## 🐛 如果还有问题

查看日志文件：
```bash
type logs\run_all_*.log
```

或联系技术支持。

---

现在就试试运行吧！🎉

